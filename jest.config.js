module.exports = {
  // Use jsdom environment for DOM-related testing
  testEnvironment: 'jsdom',

  // Specify the root directory for your tests
  roots: ['<rootDir>/tests'],

  // Automatically clear mock calls and instances between every test
  clearMocks: true,

  // Enable verbose output for more detailed test results
  verbose: true,
};
